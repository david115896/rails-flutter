<%= form_with(model: tripactivity, local: true) do |form| %>
  <% if tripactivity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tripactivity.errors.count, "error") %> prohibited this tripactivity from being saved:</h2>

      <ul>
      <% tripactivity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
